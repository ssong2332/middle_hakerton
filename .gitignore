# Environment variables — never commit real secrets
.env

# Dependencies
node_modules/

# OS artifacts
.DS_Store
Thumbs.db

# AI 개발 도구 설정 — 로컬 전용 (팀원마다 사용하는 도구가 다름)
# 디스크에는 남아 로컬에서 그대로 동작하며, 리포에만 올리지 않는다.
.claude/
