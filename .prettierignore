node_modules
dist
.next
build
coverage

# 문서는 각각 다른 소유자(architect/planner/ux-design/User)가 있다 — `docs/CodingRules.md` Style은
# 코드 포맷팅 규칙이며 산문 문서까지 대상으로 삼지 않는다. `npm run format`이 소유자 밖 문서를
# 건드리지 않도록 여기서 제외한다.
docs/**
README.md
CLAUDE.md
AGENTS.md
CHANGELOG.md

# Next.js가 빌드마다 자동 재생성한다 — 파일 상단에 "이 파일을 손으로 고치지 마라"고 명시한다.
# eslint.config.js는 이미 이 파일을 제외한다.
**/next-env.d.ts
