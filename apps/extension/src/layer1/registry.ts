/**
 * 층 2 어댑터 계약 — 🔒 Freeze Point 4(F4), `docs/Architecture.md` "동결 지점" 표.
 *
 * 🔴 이 동결 지점의 소유 태스크는 **T57**(목표 시점 M3 앞단 08-16)이며 T2(M0 스캐폴드)의 범위가 아니다.
 * 여기서 `Layer2Adapter` 인터페이스를 미리 확정하면 T57의 설계 권한을 앞지르게 된다 —
 * `docs/Architecture.md` 설계 제1원칙 R2("경계는 먼저 고정하고 내용은 나중에 채운다")가 지정한
 * 순서를 지킨다. 지금은 파일과 디렉터리 구조만 존재한다.
 */
export {};
