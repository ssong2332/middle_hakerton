# Decision Log — 크로스보더 협업 중재 서비스

Owner: architect (see AGENTS.md). One line per decision; anything with lasting structural impact gets a full ADR in adr/.
Technical/architectural decisions only — planning-level decisions (scope exclusions, priority calls) live in docs/PRD.md's Planning Decisions section, owned by planner.

## Rules
- Decisions are append-only. To reverse one, add a new row that supersedes it (and a new ADR if the original had one).
- "Why" is mandatory — a decision without a reason cannot be evaluated later.

> ✅ **승인 상태 안내 (2026-08-04)** — 아래 **#1·#3·#4·#5·#6·#7** 의 Decision 열에는 작성 시점의 `🔴 proposed` 표기가 그대로 남아 있다. **이 표기는 이력이며 현재 상태가 아니다.** 여섯 항목은 2026-08-04 사용자 결정으로 **전부 승인**되었고, 그 사실은 **#31**(신규 행)에 기록되어 있다.
> **append-only 규칙을 지키기 위해 원 행의 본문을 고치지 않았다** — 승인은 결정의 *번복*이 아니라 *상태 확정*이므로 supersede 행이 아니라 **승인 사실을 기록하는 새 행**으로 남겼다. 상태를 확인할 때는 항상 **#31을 함께 읽는다.**
> **"승인 전 T2·T3·T17·T45 착수 금지" 조항은 #31로 해제되었다.**

| # | Date | Decision | Why | ADR |
|---|---|---|---|---|
| 1 | 2026-08-04 | 🔴 **`proposed`** — 구현 언어·프레임워크를 **TypeScript 5.x(strict) + Next.js 15 App Router 통합 1리포**로 한다. 백엔드는 별도 서버가 아니라 `app/api/*` Route Handler | 마감 17일·팀 4명. 분리하면 배포 2개·CI 2개·CORS·환경변수 2벌이 생기고 그 셋업만으로 [FE] 1명의 하루가 사라진다. PRD Constraints의 "기술 스택 팀 선호: Next.js(TypeScript) 통합"과도 일치하며, AC-030(키 백엔드 경유)은 Route Handler 하나로 충족된다 | [0001](adr/0001-nextjs-integrated-monorepo-with-pure-core.md) |
| 2 | 2026-08-04 | **`packages/core` 를 npm workspaces 별도 패키지로 분리**하고, 프레임워크·DB·HTTP 클라이언트 import를 ESLint `no-restricted-imports` 로 금지한다 | AC-028이 "코어가 어댑터에 의존하지 않음을 **import 경로로 확인**"을 요구한다. 디렉터리 규칙은 리뷰 의견이지만 **패키지 경계는 빌드 실패**다. 추가로 확장이 `observation/indicators.ts` 를 서버와 **같은 정의로** 써야 한다(AC-080④). 대가는 초기 셋업 30분 | [0001](adr/0001-nextjs-integrated-monorepo-with-pure-core.md) |
| 3 | 2026-08-04 | 🔴 **`proposed`** — DB **엔진**을 **PostgreSQL 15+ (관계형)** 으로 한다 (호스팅은 #4 별건) | 스키마가 실제 관계형이고(user→profile→learned_items, pair→protocol), **AC-039(타 사용자 데이터 미조회)를 RLS로 DB 레벨에서 강제**할 수 있다 — 애플리케이션 `where` 절에 맡기면 빠뜨린 한 곳이 곧 유출이다. AC-013의 "3회" 판정도 `GROUP BY … HAVING count(*)>=3` 한 줄 | [0002](adr/0002-supabase-auth-and-rls.md) |
| 4 | 2026-08-04 | 🔴 **`proposed`** — DB **호스팅**을 **Supabase 관리형 Postgres(Free)** 로 한다. 신규 의존성 `@supabase/supabase-js`, `@supabase/ssr` | PRD Constraints "저장소는 Supabase(프로필, diff 히스토리, 용어사전)". 엔진과 호스팅은 별개 결정이므로 #3과 나눠 기록한다. 무료 티어 한도는 measured(500MB DB / 50,000 MAU / 5GB egress / 활성 프로젝트 2개 / **1주 미사용 시 일시정지** — supabase.com/pricing 2026-08-04 직접 열람) | [0002](adr/0002-supabase-auth-and-rls.md) |
| 5 | 2026-08-04 | 🔴 **`proposed`** — **인증은 Supabase Auth(이메일+비밀번호)** 를 쓰고 자체 구현하지 않는다. 인가는 **RLS**. 비밀번호 최소 8자는 **Supabase 설정에서만** 지정하고 앱에 복잡도 검증을 만들지 않는다 | **Planning Decision #30이 architect에게 넘긴 판단이다.** 자체 구현은 [BE-B] 1~2일인데 T45는 **P1이고 컷 순서 ⑤("로그인 고도화")의 대상**이다 — 잘라낼 수도 있는 기능에 비밀번호 해싱·세션 회전이라는 보안 표면을 우리가 지는 것은 비율이 맞지 않는다. Planning Decision #86이 "인증 수단이 자체 정책을 강제하면 8자 최소 충족 여부만 확인하고 중복 구현하지 않는다"고 이미 지시했다 | [0002](adr/0002-supabase-auth-and-rls.md) |
| 6 | 2026-08-04 | 🔴 **`proposed`** — 배포 대상은 **Vercel Hobby + `*.vercel.app` 기본 서브도메인**. `main` 머지 시 자동 배포, 롤백은 직전 배포 **Promote to Production** | Planning Decision #28(무료 호스팅 기본 서브도메인, 커스텀 도메인 미구매)을 그대로 만족하고, Delivery & Deployment의 "main 머지 시 자동 반영"과 "10분 이내 롤백"이 **기본 동작**으로 제공된다. ⚠️ **measured 제약**: Hobby는 **Git 조직 소유 리포에 연결할 수 없다**(vercel.com/docs/limits, 2026-08-04 직접 열람) — T17 착수 전 리포 소유 형태 확인 필수 | — |
| 7 | 2026-08-04 | 🔴 **`proposed`** — **실시간/websocket을 쓰지 않는다.** Supabase Realtime·SSE·폴링 모두 미채택, 요청/응답만 | 2패널 "수신자 도착"은 **같은 요청의 응답을 오른쪽에 렌더**하는 단일 사용자 화면이고(UX Decision Log), 규약 합의 알림은 Planning Decision #87이 MVP에서 제외했으며, 침묵 감지는 **수동 마킹**이고(AC-044⑤), 예약 발송은 mock-send다. 즉 푸시가 필요한 지점이 명세에 하나도 없다. 넣으면 구독 수명주기·재연결·중복 이벤트라는 새 실패 모드 3종이 생긴다 | — |
| 8 | 2026-08-04 | LLM 클라이언트로 **공식 `openai` npm 패키지**를 쓰고, **모델명은 `OPENAI_MODEL` 환경변수**로 뺀다 | Planning Decision #16이 OpenAI를 확정했다(user-approved). 모델명을 코드에 박으면 Decision #29(크레딧 제한 가정)에서 등급을 내릴 때 코드 변경이 필요해진다. 참고 가격(measured, developers.openai.com/api/docs/pricing 2026-08-04 직접 열람): `gpt-5-nano` $0.05/$0.40 · `gpt-4o-mini` $0.15/$0.60 · `gpt-5.5` $5.00/$30.00 per 1M tokens. ⚠️ **한↔영 중재 품질이 저가 등급에서 충분한지는 미검증** — M1 첫날 스팟체크(T5/T7/T10)로 확인 | — |
| 9 | 2026-08-04 | **LLM 응답 캐시를 Postgres `llm_cache` 테이블**에 둔다. 키 = `sha256(model ∥ promptVersion ∥ step ∥ canonicalJSON(정규화 입력))`, **user_id를 키에 넣지 않는다** | AC-041/Planning Decision #29가 "캐시 키·저장 위치 설계"를 architect에게 넘겼다. **서버리스에서 인메모리 캐시는 발표 중 적중하지 않는다**(호출마다 프로세스가 다를 수 있다). Vercel KV는 무료 티어 한도를 하나 더 만든다. **user_id를 키에서 뺀 이유**: 데모 시연은 같은 원문을 여러 번 실행하는 것이 정상이고(T35 리허설·T37 발표), 계정을 바꾼 순간 캐시가 무효가 되면 발표 직전에 크레딧을 태운다. 개인화 값은 payload에 들어가므로 내용이 다르면 키도 다르다 | — |
| 10 | 2026-08-04 | **요청 상한을 2겹**으로 둔다 — 사용자·일 단위 + **전역·일 단위**. 초과 시 오류가 아니라 **폴백 응답 200**을 반환한다 | AC-041. 사용자 단위만 두면 심사위원 계정이 여러 개일 때 전역 크레딧이 그대로 소진된다. 상한 초과를 오류로 만들면 발표 중 화면이 죽는데, AC-041이 요구하는 것은 **폴백으로 계속 동작하되 폴백임을 표시**하는 것이다 | — |
| 11 | 2026-08-04 | **폴백 응답을 `packages/core/src/data/fallback-responses.ts` 정적 데이터**로 두고, `docs/TestCases.md`·`docs/DemoScript.md` 의 시연 입력에 대응하는 응답을 **동일 cacheKey 로 미리 계산해** 담는다 | Planning Decision #29가 "소진 시 사전 준비 데모 응답 폴백"을 필수로 지정했다. cacheKey를 공유하면 **대본에 있는 장면은 크레딧 0으로도 정확히 재현**된다. DB에 두지 않는 이유: 배포 산출물과 함께 롤백돼야 하고, DB에 두면 롤백 시 코드와 데이터가 갈린다 | — |
| 12 | 2026-08-04 | 입력 검증은 **`zod`** 로 하며 **HTTP 경계(`withApi()`) 한 곳**에서만 수행한다. core 안에 재검증을 만들지 않는다 | 검증 지점이 여러 곳이면 같은 입력이 두 가지로 판정된다. core는 "이미 검증된 타입"만 받는 순수 함수로 남아야 AC-028의 프레임워크 비의존이 성립한다. zod는 OpenAI 응답·GitHub 응답 검증(외부 API 경계)에도 같은 스키마 문법을 쓴다 | — |
| 13 | 2026-08-04 | 테스트 러너는 **Vitest**, DOM 테스트는 `@testing-library/react`. 테스트 위치는 소스 옆 `*.test.ts` | T11이 **26건을 "하나의 실행 출력"으로** 보고할 것을 요구한다 — 웹앱·확장·코어가 서로 다른 러너를 쓰면 그 요구를 만족할 수 없다. Vite 기반이라 확장 번들러와 설정을 공유한다. DoD Gate의 "red/green 출력 첨부"도 러너가 하나여야 실행 가능하다 | — |
| 14 | 2026-08-04 | 린터 **ESLint(flat config)** + 포매터 **Prettier**. 설정 파일은 리포 루트(`eslint.config.js`, `.prettierrc`) | `docs/CodingRules.md` Style 칸이 비어 있어 **DoD Gate "Lint passes"가 현재 실행 불가**이고, 그대로 두면 매 릴리스가 사용자 승인 skip을 필요로 한다. ESLint를 택한 결정적 이유는 `no-restricted-imports` 로 **#2의 코어 순수성을 빌드 실패로 강제**할 수 있다는 것 — 대안(Biome)은 빠르지만 이 규칙의 성숙도가 확인되지 않았다(추정) | — |
| 15 | 2026-08-04 | Chrome 확장 번들러는 **Vite(라이브러리 모드)**, Manifest V3 | 개발자 모드 로드 확정(Planning Decision #4)이라 스토어 패키징 요구가 없다. Vite는 `packages/core` 를 그대로 번들할 수 있어 AC-080④(지표 정의 공유)가 추가 설정 없이 성립한다 | — |
| 16 | 2026-08-04 | **ORM을 쓰지 않는다.** `@supabase/supabase-js` + `supabase gen types` 로 생성한 타입만 사용 | ORM은 스키마 정의처를 **마이그레이션과 이중화**한다 — Freeze Point F3가 두 개가 되는 것이 17일에서 가장 비싼 실수다. 테이블 11개·조인 2~3곳 규모에서 ORM이 절약하는 타이핑보다 동기화 비용이 크다 | — |
| 17 | 2026-08-04 | **확장은 Bearer 토큰으로 인증**한다. 웹앱 `/extension/connect` 페이지가 `chrome.runtime.sendMessage` 로 access token을 넘기고, 확장은 `chrome.storage.session` 에 보관한다. `externally_connectable` 은 **우리 앱 origin 1개로 제한** | Supabase 세션 쿠키는 `SameSite=Lax` 라 확장의 교차 사이트 요청에 실려 가지 않는다(**추정** — 확인 수단: T56 착수 시 네트워크 탭에서 쿠키 헤더 유무 관찰). **기각한 대안**: 층 1 패널을 우리 origin iframe으로 띄워 쿠키를 1st-party로 쓰는 안 — 대상 사이트의 `frame-src` CSP가 막을 수 있고 그것이 정확히 AC-052가 걱정하는 실패 모드다 | — |
| 18 | 2026-08-04 | 층 2 어댑터를 **배열 등록형 레지스트리**(`Layer2Adapter[]`)로 만든다. 층 1은 층 2를 import 하지 않고, 층 2 어댑터끼리도 서로 참조하지 않는다 | Planning Decision #62/#68의 컷 순서를 **파일 삭제 + 배열 한 줄 제거**로 실행 가능하게 만드는 유일한 구조다. AC-053③("층 2 전부 제거 상태에서 층 1 전 경로 동작")을 1회 실행으로 증명할 수 있다. 어댑터끼리의 참조를 금지한 이유: #68이 Slack·Gmail을 동순위로 두고 스파이크 결과로 하나만 남길 수 있게 했는데, 서로를 참조하면 하나를 지울 때 다른 하나가 깨진다 | [0003](adr/0003-layer1-layer2-adapter-registry.md) |
| 19 | 2026-08-04 | 쌍방 규약을 **`pair_key`(정렬·소문자화한 두 식별자) UNIQUE 한 행**으로 저장하고, `auth.users` 와 FK로 묶지 않는다 | AC-037이 "양측이 열람·수정"을 요구하므로 소유자별 2행이 아니라 **한 쌍 1행**이어야 한다. FK를 걸지 않는 이유: **상대가 미가입일 수 있다**(AC-065① — 자유 텍스트 이메일만으로 중재가 정상 완료된다). 접근 제어는 RLS가 `auth.jwt()->>'email'` 과 대조해 수행한다 | — |
| 20 | 2026-08-04 | AC-074④의 충돌 검사를 **조건부 UPDATE**(`WHERE … AND authorship_state <> 'counterpart_authored'` + 영향 행 수 확인)로 구현한다. 시각 비교를 쓰지 않는다 | AC-075③가 "시각 비교로 판정하지 않는다"를 명시했고, UX-018은 "race-condition guard, **not merely a pre-check**"를 요구했다. Stage 3 진입 시 사전 검사만으로는 Stage 3~4 사이의 경합을 막지 못한다 — `WHERE` 절이 그 원자성을 준다 | — |
| 21 | 2026-08-04 | `pair_protocols.authorship_state` 의 **`inference_draft`(ⓐ)는 MVP 쓰기 경로가 절대 생성하지 않는다.** 레코드는 `untouched` → `sender_confirmed` 로 직접 전이하며, ⓐ는 렌더 가능한 CHECK 값으로만 남긴다. 이력 테이블을 만들지 않는다 | **AC-075가 "컬럼명·타입·정규화·이력 보관 여부는 architect 몫"으로 남긴 판단이고**, UX-011 Architect Handoff가 "ⓐ가 언제 영속 읽기로 관측되는가"를 유일한 잔여 모호성으로 지목했다. AC-074②가 확정 전 저장을 금지하므로 초안은 세션 안에만 존재한다 — 따라서 ⓐ가 저장된 행은 MVP에서 0건이다. AC-075는 "마지막으로 수정한 주체"만 요구하고 이력은 요구하지 않는다 | — |
| 22 | 2026-08-04 | 용어사전 스코프를 **사용자 단위**로 확정한다. 미사용 `workspace_id` 컬럼도 만들지 않는다 | UX-010이 "project가 team/workspace에 대응하는지는 architect 결정"으로 남겼다. PRD 어디에도 팀/워크스페이스 개념이 없어 만들면 **PRD에 없는 기능을 추가하는 것**이 되고(AGENTS.md 금지), AC-016은 "사용자가 추가·수정·삭제"만 요구한다. 팀 스코프는 post-MVP의 additive 마이그레이션 1건 | — |
| 23 | 2026-08-04 | **관측 표본 삭제 시 지표를 남은 표본에서 재집계**한다(누적 카운터 감산이 아니다). 지표를 캐시하지 않고 조회 시점에 집계한다 | AC-081④가 "삭제 시 해당 표본이 지표 산출에서 빠짐"을 검증 대상으로 삼는다. 감산 방식은 부동소수·순서 문제로 어긋날 수 있고, 표본 규모(상대당 수십 건)에서 재집계 비용이 무의미하다 | — |
| 24 | 2026-08-04 | **P2 테이블(`recipient_enrichments`, `observation_samples`)의 스키마도 최초 마이그레이션 0001에 포함**한다 | `docs/Tasks.md` T1이 이미 같은 판단을 했다 — *"ⓑⓒ는 P2·P1 기능이 채우지만 스키마는 지금 확정한다. 나중에 스키마를 바꾸면 프론트·백엔드 통합 재작업이 생긴다."* 테이블이 있어도 쓰는 코드가 없으면 비용은 0이고, 컷되면 빈 테이블이 남을 뿐이다 | — |
| 25 | 2026-08-04 | **2026-08-19 00:00 KST 이후 마이그레이션은 additive-only** — `ADD COLUMN`(nullable) / `CREATE TABLE` / `CREATE INDEX` 만 허용, `DROP`·`RENAME`·`ALTER TYPE`·NOT NULL 추가 금지 | **롤백 때문이다.** Supabase Free에 시점 복구가 없고 down 마이그레이션을 검증할 시간도 없다. 컬럼을 지우지 않으면 **구버전 코드가 신버전 스키마 위에서 그대로 동작**하므로 코드만 되돌려도 시스템이 정합해진다. Delivery & Deployment의 "10분 이내 롤백" 요구가 이 설계 제약을 만든다 — 되돌릴 수 없는 시스템은 다르게 설계해야 한다 | — |
| 26 | 2026-08-04 | **에러 추적 서비스(Sentry 등)를 도입하지 않되**, LLM 호출 결과를 `llm_call_log` 테이블에 **내용 없이** 기록한다 | ⚠️ 로그만으로는 부족하다는 것이 measured로 확인됐다 — **Vercel Hobby의 런타임 로그 보존은 1시간**이다(vercel.com/docs/limits, 2026-08-04 직접 열람). 발표 2시간 전의 실패는 로그에서 사라진다. `llm_call_log` 는 폴백 비율·p95 지연·에러 코드를 남겨 "왜 폴백이 떴는가"에 답할 수 있는 유일한 증거가 된다. Sentry 미도입 이유: 계정·DSN·소스맵까지 반나절이고, 17일에서 그 반나절은 T55 스파이크 하나와 같은 값이다 | — |
| 27 | 2026-08-04 | **로그에 메시지 원문·변환문·역번역문·관측 표본 원문·이메일 주소를 넣지 않는다.** 길이는 `inputChars` 숫자로만 남긴다. 로그 `event` 이름은 `docs/UX.md` 각 화면의 Events Emitted 를 그대로 쓴다 | 업무 메시지와 제3자 발언이 로그에 남는 순간 AC-081②③의 "원문 미저장·서버 미전송" 방어선이 **로그 쪽으로 뚫린다.** CLAUDE.md의 시크릿 로그 금지와 같은 계열이되 대상이 더 넓다. 이벤트 이름을 UX.md에서 가져오면 로그와 UX 명세가 자동으로 정합한다 | — |
| 28 | 2026-08-04 | 로컬·프로덕션이 **Supabase 프로젝트 1개를 공유**한다. 개발용/데모용 계정을 **계정 단위로 분리**하고 RLS가 그것을 강제한다 | Free 티어의 활성 프로젝트가 2개이고(measured), 17일 안에 2환경 스키마를 동기화하는 비용이 이득보다 크다. **대가를 숨기지 않는다**: 로컬 개발이 프로덕션 데이터에 쓴다. 완화는 계정 분리 + RLS이며, 데모 시드(T61·T62)는 전용 계정에만 넣는다 | — |
| 29 | 2026-08-04 | 데모 시드를 **마이그레이션이 아니라 별도 스크립트**(`scripts/seed-demo.ts`)로 두고 배포 파이프라인에 넣지 않는다 | 시드가 배포에 끼면 롤백 시 코드와 데이터 상태가 갈린다(#25와 같은 이유). T61·T62는 사람이 1회 실행하는 작업이다 | — |
| 30 | 2026-08-04 | **국가·국민성 데이터를 DB에 만들지 않는다.** 이모지 위험도 3단계 룩업과 공휴일 데이터는 `packages/core/src/data/` 정적 파일이며 `country`/`region`/`nationality` 컬럼을 갖지 않는다 | AC-056①이 "스키마·데이터 파일 grep으로 확인"을 판정 방법으로 지정했고 Planning Decision #6/#71이 국가 스테레오타입 사용을 금지했다. 공휴일을 DB에 두지 않는 이유는 별도로 하나 더 있다 — AC-048①의 "외부 API 호출 0"과 같은 취지로, DB에 두면 시드가 배포 단계에 끼어들고 롤백 시 불일치가 생긴다 | — |
| 31 | 2026-08-04 | ✅ **기술 기반 6개 항목이 사용자 결정으로 전부 승인되었다** — ① 언어/프레임워크(#1) ② DB 엔진(#3) ③ DB 호스팅(#4) ④ 배포 대상(#6) ⑤ 인증 방식(#5) ⑥ 실시간 미채택(#7). **architect 권고안 그대로 채택**되었으며 수정된 항목은 없다. <br>**따라서 "승인 전 T2·T3·T17·T45 착수 금지" 조항을 해제한다.** <br>⚠️ **#1·#3·#4·#5·#6·#7 의 본문은 수정하지 않았다** — 승인은 결정의 *번복*이 아니라 *상태 확정*이므로 append-only 규칙상 supersede 행이 아니라 **본 행으로 사실을 기록**한다. 원 행의 `🔴 proposed` 표기는 작성 시점 이력이며 현재 상태가 아니다. <br>**사용자 조건 2건의 충족 확인**: ②③은 "무료로 가능하면", ④는 "무료로 진행 가능하면"이 조건이었고 **둘 다 $0으로 확인**되었다(아래 근거) | 사용자 결정 2026-08-04. **승인 조건의 충족 근거(전부 measured — 오케스트레이터가 2026-08-04 해당 페이지를 직접 열람)**: **Supabase Free**(supabase.com/pricing) = $0/month · DB 500MB · **50,000 MAU** · egress 5GB(+cached 5GB) · **파일 저장 1GB** · **활성 프로젝트 2개** · **1주 미사용 시 일시정지**. **Vercel Hobby**(vercel.com/docs/plans/hobby) = **$0, 청구 주기 없음** · Function Invocations 100만 · Edge Requests 100만 · 배포 100회/일 · 프로젝트 200개 · **함수 최대 300초** · **Runtime Logs 1시간** · 한도 초과 시 대체로 30일 대기. <br>**#4 의 Why 열이 인용한 무료 한도에서 누락된 항목 1건을 여기서 보완한다 — 파일 저장 1GB.** 우리 스키마에 **파일 업로드가 없고**(PRD 전체 AC에 업로드 요구 0건, Supabase Storage 미사용) 따라서 **해당 없음**이다. <br>**#28(로컬·프로덕션 프로젝트 1개 공유)의 직접 근거는 "활성 프로젝트 2개"** 이다 — 2개를 환경 분리에 다 쓰면 여유분이 0이 되어 사고 시 복구용 프로젝트를 만들 수 없다 | [0001](adr/0001-nextjs-integrated-monorepo-with-pure-core.md) · [0002](adr/0002-supabase-auth-and-rls.md) |
| 32 | 2026-08-04 | 🔴 **Vercel Hobby의 "비상업·개인 사용 전용" 제약을 `추정` 이 아니라 `measured` 로 등재한다.** MVP 기간(~2026-08-21)에는 차단 요인이 **아니며**, **상용화 시점에 Pro($20/user/월) 전환이 필요**하다는 사실을 함께 고정한다 | **원문 확인(measured, vercel.com/docs/plans/hobby, 2026-08-04 오케스트레이터 직접 열람)**: *"the Hobby plan restricts users to **non-commercial, personal use only**."* 나는 앞선 설계 패스에서 이것을 `추정`으로 표기했으나 **확정 사실이다.** <br>**MVP에 걸리지 않는 이유**: 해커톤 제출·시연은 비상업이다. **걸리는 시점**: Planning Decision #27(실사용자 확장)과 Open Question #6 결정(좌석당 $8~12 구독 — Monetization user-approved)이 실행되는 순간 정면으로 걸린다. <br>**즉 #6(Vercel Hobby 채택)은 MVP 한정 유효한 결정이며 수익 모델과 함께 재검토 대상**이다. 이 사실을 발표에서 "무료로 운영 중"이라고만 말하면 AC-034(미구현·미확정을 확정처럼 보이지 않게)와 같은 계열의 과장이 된다 | — |
| 33 | 2026-08-04 | Open Question #1(리포 소유 형태)이 **해소되었다 — Vercel Hobby 차단 요인 아님.** 단 제약 자체는 문서에서 삭제하지 않고 "우리에게는 해당 없음"으로 남긴다 | **확인(measured, 2026-08-04 오케스트레이터 직접 실행)**: 리모트 `https://github.com/ssong2332/middle_hakerton.git`, `gh api users/ssong2332 --jq .type` → **`User`**. 소유자가 **조직이 아니라 개인 계정**이므로 *"Hobby는 조직 소유 리포에 연결 불가"*(vercel.com/docs/limits) 제약에 해당하지 않는다. <br>**제약을 삭제하지 않는 이유**: 제약은 실재하며 **리포를 조직으로 이관하는 순간 즉시 발동**한다. 지웠다가 나중에 이관하면 아무도 이 위험을 다시 발견하지 못한다 | — |
| 34 | 2026-08-04 | 사용자 지시로 **`docs/CodingRules.md` 초안을 architect가 작성**한다(Naming / Directory Rules / Style / Error Handling / Tests). 설정 파일 자체(`eslint.config.js` 등)는 만들지 않는다 | AGENTS.md:102의 소유자 표기가 **"User (or architect on request)"** 이고, 2026-08-04 사용자가 "architect에게 초안 작성 지시" 선택지를 명시적으로 택해 **on request 조건이 충족**되었다. <br>**설정 파일을 만들지 않는 이유**: architect는 코드·설정 파일을 생성하지 않는다(AGENTS.md Authority "Technical design only"). 규칙 문서는 architect, 그 규칙을 실행 가능한 설정으로 옮기는 것은 **implementer/T2** 의 일이다. <br>**규칙의 판정 가능성 원칙**: 각 규칙은 "무엇이 위반인가"가 판정 가능해야 하며, 지킬 수 없거나 검증할 수 없는 규칙은 넣지 않는다 | — |
